# [Investigation title] — Report

<!-- VERSION PROTOCOL (Principle P8 (the-method-matures-backward)):
     Corrections are applied IN PLACE and stamped in the errata table below.
     Never file a correction beside the original text — an orphaned correction
     gets overwritten by the original the next time someone copies from it.
     On any external objection: record it here (concession or parry), then
     RECOMPUTE what survives — re-run the salience map and the verdict table,
     don't patch the single point (Principle P3 (inherited-salience-is-not-importance)). -->

## Errata & versions

| Version | Date | External objection (verbatim gist) | Concession / parry (with reason) | What was recomputed |
|---------|------|-------------------------------------|----------------------------------|---------------------|
| v1 | YYYY-MM-DD | — | — | initial |

## Metadata & limits

<!-- What this report can and cannot claim: date range of evidence, languages
     actually searched (and languages that SHOULD have been but weren't — a
     named gap, not a silence), budget/depth mode, saturation threshold used,
     where the sweep stayed thin. An unreachable vein (firewalled, suppressed)
     is a finding — list it here, not nowhere. -->

## Compressed verdict

<!-- 3–6 sentences max. Verdict tiers match evidence tiers (no mainstream
     confirmation = UNVERIFIED, not FALSE). State the cost-of-being-wrong. -->

## Chronology

| Date | Event | Evidence tier | Source (primary?) |
|------|-------|--------------|-------------------|

## Per-claim verdict table

| # | Claim | Tier (+[RELAY] if unverified relay) | Verdict | What would change it |
|---|-------|--------------------------------------|---------|----------------------|

<!-- Load-bearing claims may not remain [RELAY] (Principle P1 (relayed-is-not-read)). Every verdict
     carries a falsifier — a verdict with no "what would change it" is a
     belief, not a finding. -->

## Satellite claims

<!-- Claims that ride along with the load-bearing ones but carry no verdict
     weight. Demoted [RELAY] claims land here. Keeping them visible-but-
     separate is the alternative to silently dropping or silently crediting
     them. -->

## Horizontal source analysis

<!-- Per source USED (not per claim): position, funding/ownership trace,
     what it was used for, what it is silent about. Symmetric: the trace
     applies to debunkers and anti-disinfo organizations exactly as to
     everyone else (Principle P4 (counter-default)). -->

## Symmetric cui-bono

<!-- Beneficiary mapping for EVERY side of every load-bearing claim,
     including the correction/debunking narrative (cui-bono §2a). -->

## Per-camp omissions

<!-- For each camp: what it does not mention. Omission ≠ falsity of the
     omitted — it locates what is inconvenient where. -->

## Hypotheses, parsimony-ranked

| Rank | Hypothesis | Explains | Fails to explain | Assumptions required |
|------|-----------|----------|------------------|----------------------|

<!-- Ranked by assumptions required, not by comfort. Intent-attributions
     are UNVERIFIED by construction — mark them. -->

## Decision points

<!-- What the reader can actually decide/do differently given each verdict,
     and which open question would change which decision. This section is
     why the report exists; if it is empty, question the investigation. -->

## Annexes

<!-- Raw notes, search logs, full quotes, channel notes. Process artifacts
     live here, never in the sections above. -->
